<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>LUNI - Unified Game Launcher</title>
  </head>
  <body>
    <div id="root" class="bg-slate-200 dark:bg-slate-800 dark:text-white"></div>
  </body>
  <script>
    // console.log(window.api);
    let theme = window.api.store.get('theme');
    document.documentElement.className = theme;

    window.api.ping.once('ipc-example', (arg) => {
      // eslint-disable-next-line no-console
      console.log(arg);
    });

    window.api.ping.myPing();
  </script>
</html>
